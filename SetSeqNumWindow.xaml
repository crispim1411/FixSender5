<Window x:Class="FixSender5.SetSeqNumWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Sequence Numbers" 
        Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>
        
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Set New Sequence Numbers" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>
        
        <!-- Input Fields -->
        <Border Grid.Row="1" 
                BorderBrush="#FFDDDDDD" 
                BorderThickness="1" 
                CornerRadius="5" 
                Background="#FFFAFAFA"
                Padding="20">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Incoming SeqNum -->
                <Label Grid.Row="0" Grid.Column="0" 
                       Content="Incoming SeqNum:" 
                       Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         x:Name="InSeqNumTextBox"
                         Style="{StaticResource TextBoxStyle}"
                         ToolTip="Enter the next incoming sequence number (1-999999)"/>
                
                <!-- Outgoing SeqNum -->
                <Label Grid.Row="1" Grid.Column="0" 
                       Content="Outgoing SeqNum:" 
                       Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         x:Name="OutSeqNumTextBox"
                         Style="{StaticResource TextBoxStyle}"
                         ToolTip="Enter the next outgoing sequence number (1-999999)"/>
                
                <!-- Info -->
                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="Note: Changes will take effect after reconnection"
                           FontSize="11"
                           Foreground="#FF666666"
                           FontStyle="Italic"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,20,0,0">
            <Button Content="Cancel" 
                    x:Name="CancelButton"
                    Click="OnCancelClick"
                    Background="#FF6C757D"
                    Foreground="White"
                    Style="{StaticResource ButtonStyle}"/>
            
            <Button Content="OK" 
                    x:Name="OkButton"
                    Click="OnOkClick"
                    Background="#FF0078D4"
                    Foreground="White"
                    Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>