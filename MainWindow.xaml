<Window x:Class="FixSender5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="0" Height="50" Click="OnClickInitiator">Initiator</Button>
        <Button Grid.Column="1" Height="50" Click="OnClickAcceptor">Acceptor</Button>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label>Status:</Label>
            <Label Content="{Binding Path=Status}"/>
        </StackPanel>
    </Grid>
</Window>
